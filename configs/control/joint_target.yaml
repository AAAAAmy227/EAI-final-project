# Joint Target Delta Position Control (Recommended for Sim2Real)
# Action: 6 dim (5 arm + 1 gripper)
# Actions are delta from TARGET joint positions (trajectory tracking)
control_mode: pd_joint_target_delta_pos
normalize_action: true
delta_arm: 0.05      # rad per step
delta_gripper: 0.2   # rad per step
